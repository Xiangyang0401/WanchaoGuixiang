# Godot 4+ 引擎缓存与导入产物
.godot/
/android/

# Godot 导出配置（含本机路径，不入库）
export.cfg
export_presets.cfg
*.translation

# Python 工具缓存（tools/*.py 的运行产物）
__pycache__/
*.py[cod]

# 开发过程产物：调色/裁切/动画分段的比对截图
.preview/
